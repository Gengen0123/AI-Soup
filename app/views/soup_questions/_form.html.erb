<%= form_with(model: soup_question) do |form| %>
  <% if soup_question.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(soup_question.errors.count, "error") %> prohibited this soup_question from being saved:</h2>

      <ul>
        <% soup_question.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.textarea :body %>
  </div>

  <div>
    <%= form.label :answer, style: "display: block" %>
    <%= form.textarea :answer %>
  </div>

  <div>
    <%= form.label :explanation, style: "display: block" %>
    <%= form.textarea :explanation %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
